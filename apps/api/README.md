# apps/api

Minimal Express API for IUM-MORAVE (prototype scaffold)

Run locally:

1. From repository root, install workspace deps:
   npm install

2. Start API:
   cd apps/api
   node index.js

Environment variables
- Create apps/api/.env from apps/api/.env.example and set values as needed.
